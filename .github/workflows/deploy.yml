# Reusable workflow. Picked up by the "deploy" stage (file-name prefix "deploy").
# pipeline-secrets: inherit   # opt in to receive the caller's secrets (deploy needs creds)
name: Deploy

on:
  workflow_call:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        run: |
          echo "TODO: deploy / release / publish"
