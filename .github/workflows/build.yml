# Reusable workflow. Picked up by the "build" stage (file-name prefix "build").
name: Build

on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: |
          echo "TODO: build / package your artifact"
